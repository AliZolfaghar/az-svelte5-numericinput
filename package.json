{
  "name": "az-svelte5-numeric-input",
  "version": "1.0.13",
  "description": "Svelte 5 Numeric Input component with runes",
  "svelte": "./dist/index.js",
  "exports": {
    ".": {
      "svelte": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "copy": "cp src/*.svelte dist/ && cp src/index.js dist/",
    "build": "npm run clean && vite build && npm run copy",
    "dev": "vite",
    "preview": "vite preview"
  },
  "peerDependencies": {
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.1.4",
    "svelte": "^5.0.0",
    "vite": "^7.1.5"
  },
  "type": "module"
}
